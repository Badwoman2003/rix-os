[package]
name = "os"
version = "0.1.0"
edition = "2024"

[build-dependencies]
bootloader = "0.11"
kernel = { package = "kernel", path = "kernel", artifact = "bin", target = "x86_64-unknown-none"}

[workspace]
resolver = "3"
members = ["kernel"]

[dependencies]
ovmf-prebuilt = "0.1.0-alpha.1"
